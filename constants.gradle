project.ext {
    submodules = ['database', 'auth', 'storage', 'firestore', 'common']
    group = 'com.firebaseui'
    version = '3.1.0'
    pomdesc = 'Firebase UI Android'

    compileSdk = 28
    targetSdk = 28
    minSdk = 14

    buildTools = '28.0.2' // Remember to also update in .travis.yml

    firebaseVersion = '11.4.2'
    supportLibraryVersion = '28.0.0'
    architectureVersion = '1.0.0-beta1'
}
