project.ext {
    submodules = ['database', 'auth', 'storage']
    group = 'com.firebaseui'
    version = '2.1.0'
    pomdesc = 'Firebase UI Android'

    compileSdk = 25
    targetSdk = 25
    minSdk = 14

    buildTools = '25.0.3' // Remember to also update in .travis.yml

    firebaseVersion = '11.0.6'
    supportLibraryVersion = '25.4.0'
}
